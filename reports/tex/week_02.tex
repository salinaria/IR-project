\section{Week 2: Indexing and collection analysis}
Build PyTerrier index under \texttt{index/terrier}. Run collection statistics: file-type table, vocabulary and token counts, Zipf log-log plot, document-length distribution, stopword table, word cloud---all under \texttt{outputs/analysis/}. Crawl is HTML-only; PDF/DOCX would need a separate ingest.
