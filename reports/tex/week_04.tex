\section{Week 4: Pooling and relevance}
TREC-style pool (BM25 + TF-IDF + QL, depth 10) $\rightarrow$ \texttt{pooled\_candidates.tsv}. Graded judgments in \texttt{qrels.tsv}; Cohen's kappa export in \texttt{outputs/eval/cohen\_kappa.csv}; bootstrap rank-1 file kept for sanity checks.
