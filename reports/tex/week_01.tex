\section{Week 1: Data collection}
Crawl \texttt{temple.edu} with politeness delay; stream to \texttt{data/raw/corpus.jsonl}. Resume scans the file once for max \texttt{docno} suffix and known URLs (memory-safe). Deduped count for this snapshot: \textbf{{\DedupDocs}} unique documents (from \textbf{{\RawCorpusLines}} raw lines).
